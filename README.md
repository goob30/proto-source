Source code for my protogen helmet lives in `src/`. Tools lives in [take a guess where].

In source, each screen is created in its respective category (e.g `clockScreen.cpp`, `settingsScreen.cpp`) and the `screens.cpp` handler calls them from their headers.

TODO: update ts